# Two-node GB10 DSpark. Same compose file on both Sparks; swap env file.
#   cp docker/gb10.env.example docker/gb10.head.env
#   cp docker/gb10.env.example docker/gb10.worker.env
# Start worker first, then head:
#   docker compose --env-file docker/gb10.worker.env -f docker/compose.gb10.yml up -d
#   docker compose --env-file docker/gb10.head.env   -f docker/compose.gb10.yml up -d

name: vllm-gb10-dspark

services:
  vllm:
    image: ${VLLM_GB10_IMAGE:-vllm-gb10:dspark}
    pull_policy: never
    network_mode: host
    ipc: host
    shm_size: "64gb"
    gpus: all
    devices:
      - /dev/infiniband:/dev/infiniband
    ulimits:
      memlock: -1
      stack: 67108864
    volumes:
      - ${HF_CACHE:?set HF_CACHE in the env file}:/root/.cache/huggingface
      - ${HF_CACHE}/flashinfer:/root/.cache/flashinfer
    environment:
      HF_HOME: /root/.cache/huggingface
      VLLM_USE_DEEP_GEMM: "0"
      TORCH_CUDA_ARCH_LIST: "12.1a"
      FLASHINFER_CUDA_ARCH_LIST: "12.1a"
      NCCL_IB_DISABLE: ${NCCL_IB_DISABLE:-0}
      NCCL_IB_HCA: ${NCCL_IB_HCA}
      NCCL_SOCKET_IFNAME: ${NCCL_SOCKET_IFNAME}
      GLOO_SOCKET_IFNAME: ${NCCL_SOCKET_IFNAME}
      NCCL_DEBUG: ${NCCL_DEBUG:-WARN}
    command:
      - bash
      - -lc
      - >
        exec vllm serve ${DSPARK_MODEL:-deepseek-ai/DeepSeek-V4-Flash-0731}
        --trust-remote-code
        --tokenizer-mode deepseek_v4
        --tool-call-parser deepseek_v4
        --enable-auto-tool-choice
        --reasoning-parser deepseek_v4
        --kv-cache-dtype fp8_ds_mla
        --block-size 256
        --tensor-parallel-size 2
        --nnodes ${NNODES:-2}
        --node-rank ${NODE_RANK}
        --master-addr ${MASTER_ADDR}
        --master-port ${MASTER_PORT:-29500}
        --distributed-executor-backend mp
        --gpu-memory-utilization ${GPU_MEMORY_UTILIZATION:-0.85}
        --max-model-len auto
        --max-num-seqs ${MAX_NUM_SEQS:-8}
        --max-num-batched-tokens ${MAX_NUM_BATCHED_TOKENS:-8192}
        --enable-prefix-caching
        --max-cudagraph-capture-size 64
        --compilation-config '{"cudagraph_mode":"FULL_AND_PIECEWISE","custom_ops":["all"]}'
        --speculative-config '{"method":"dspark","num_speculative_tokens":5,"draft_sample_method":"probabilistic"}'
        --host 0.0.0.0
        --port ${VLLM_PORT:-8000}
        ${HEADLESS}
